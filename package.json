{
  "name": "castify",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ castify",
    "create-local": "graph create --node http://localhost:8020/ castify",
    "remove-local": "graph remove --node http://localhost:8020/ castify",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 castify"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.2",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
