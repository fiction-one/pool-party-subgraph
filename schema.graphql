type FID @entity {
  id: ID!
  custodyAddr: Bytes! # address
  createdAtBlock: BigInt!
  createdAtTs: BigInt!
}

type FName @entity {
  id: ID!
  fname: String!
  custodyAddr: Bytes! # address
  createdAtBlock: BigInt!
  createdAtTs: BigInt!

  recoveryAddr: Bytes # address
  expiryTs: BigInt
}

type User @entity {
  id: ID!
  fid: FID
  fname: FName
}

type Count @entity {
  id: ID!
  count: BigInt
}